<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Reading device media -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Only needed on Android 9 and below for saving edited photos/videos and permanent
         delete; Android 10+ scoped storage lets the app create/write its own new media
         files without this. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Background sync -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- In-app updater: there's no Play Store, so the app installs its own downloaded update APK. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".GalleryBackupApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GalleryBackup">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.GalleryBackup"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Catches the browser redirect after OneDrive sign-in (see OneDriveAuthManager).
                 Registered in Azure as a "Mobile and desktop applications" redirect URI,
                 which accepts a plain custom scheme with no signing-key hash. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="mediahub" android:host="oauth" android:path="/onedrive" />
            </intent-filter>
        </activity>

        <!-- WorkManager schedules and survives reboot on its own; no custom boot receiver needed.
             Its default self-initialization normally runs via this same androidx.startup
             ContentProvider, which Android creates BEFORE Application.onCreate() -- before
             GalleryBackupApp has installed CrashReporter or done anything else. If that init
             ever throws (e.g. a corrupted persisted job), the app crash-loops on every single
             launch with no crash ever reaching CrashReporter, and no way back in short of
             clearing all app data. Removing WorkManager's entry here and initializing it
             ourselves in GalleryBackupApp.onCreate() (after CrashReporter is installed, and
             inside a try/catch) closes that gap. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Hands a downloaded update APK to the system installer via a content:// Uri instead of a
             raw file:// path (blocked by FileUriExposedException on Android 7+). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
